import { FC } from "react";

type ContactProps = {};

const Contact: FC<ContactProps> = (props) => {
  return (
    <div id="projects" className="w-full min-h-screen h-full relative z-40 mt-12 sm:mt-5">
      <h1 className="text-red-800">Contact</h1>
    </div>
  );
};
export default Contact;
